server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/incidents_db
    username: postgres
    password: postgres123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
